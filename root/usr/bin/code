#! /bin/bash

BIN=/usr/share/code/bin/code

# Wayland check
if [ "${PIXELFLUX_WAYLAND,,}" == "true" ]; then
  WAYLAND="--ozone-platform=wayland"
fi

dbus-launch ${BIN} \
  --no-sandbox \
  ${WAYLAND} \
   "$@"
